{
  "name": "tp-api-wrapper",
  "version": "0.0.1",
  "description": "Targetprocess REST API wrapper",
  "author": "Guillaume Oriol <guillaume@go-on.blog>",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "lint": "jslint --edition=latest --fudge \"src/**/*.js\"",
    "lint:test": "jslint --edition=latest --fudge \"test/**/*.js\"",
    "test": "npm run lint:test && npm run lint && mocha test --recursive"
  },
  "repository": {
    "type": "git",
    "url": "git@bitbucket:guillaume_oriol/targetprocess.git"
  },
  "dependencies": {
    "mout": "^1.1.0",
    "request": "^2.83.0",
    "request-promise-native": "^1.0.5",
    "when": "^3.7.8"
  },
  "devDependencies": {
    "chai": "latest",
    "chai-as-promised": "latest",
    "jslint": "latest",
    "mocha": "latest"
  },
  "keywords": [
    "targetprocess"
  ]
}
