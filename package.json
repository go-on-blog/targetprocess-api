{
  "name": "targetprocess-api",
  "version": "0.3.0",
  "description": "Targetprocess API wrapper",
  "author": "Guillaume Oriol <guillaume@go-on.blog> (https://github.com/go-on-blog)",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "cover": "istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec",
    "lint": "jslint --edition=latest --fudge \"*.js\"",
    "lint:test": "jslint --edition=latest --fudge \"test/**/*.js\"",
    "test": "npm run lint:test && npm run lint && mocha test --recursive"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/go-on-blog/targetprocess-api.git"
  },
  "bugs": {
    "url": "https://github.com/go-on-blog/targetprocess-api/issues"
  },
  "dependencies": {
    "@stamp/it": "^1.0.3",
    "mout": "^1.1.0",
    "request": "^2.87.0",
    "request-promise-native": "^1.0.5"
  },
  "devDependencies": {
    "chai": "latest",
    "chai-as-promised": "latest",
    "codecov": "latest",
    "istanbul": "latest",
    "jslint": "latest",
    "mocha": "^5.2.0",
    "sinon": "latest"
  },
  "engines": {
    "node": ">=6.0.0"
  },
  "keywords": [
    "targetprocess",
    "api"
  ]
}
